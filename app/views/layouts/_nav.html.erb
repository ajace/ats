<div class="navbar navbar-fixed-top">
    <div class="container">
      <a class="brand" href="#">Company</a>
        <div class="btn-group pull-right">
          <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">Admin <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <% if current_user %>
                <li>
                Logged in as <%= current_user.email %><br>
                <%= link_to "Admin Log Out", logout_path %>
                </li>
              <% else %>
                <li>
                <%= link_to "Admin Log In", login_path %>
                </li>
              <% end %> 
            </ul>
        </div>
    </div>
</div>