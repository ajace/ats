<%= simple_form_for @job, html: { class: "form-horizontal" } do |f| %>

	<%= f.input :name, required: false %>
	<%= f.input :description, required: false %>
  
	<%= f.button :submit, class: "btn-primary controls" %>
  
<% end %>
